<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp"
    app:cardBackgroundColor="@color/white">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Header: Title and Top Icons -->
        <TextView
            android:id="@+id/searchResultTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="HR-POL-001: Employee Onboarding"
            android:textColor="@color/text_primary"
            android:textSize="16sp"
            android:textStyle="bold"
            android:maxLines="2"
            android:ellipsize="end"
            android:layout_marginEnd="8dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/btnDownload" />

        <ImageView
            android:id="@+id/searchResultBookmark"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/ic_bookmark_border"
            app:tint="@color/brand_blue"
            app:layout_constraintEnd_toStartOf="@+id/btnDownload"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_marginEnd="12dp" />

        <ImageView
            android:id="@+id/btnDownload"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/ic_download"
            app:tint="@color/brand_blue"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Description -->
        <TextView
            android:id="@+id/searchResultDescription"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Comprehensive guide for integrating new employees into the company, covering..."
            android:textColor="@color/text_secondary"
            android:textSize="14sp"
            android:layout_marginTop="8dp"
            android:maxLines="3"
            android:ellipsize="end"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/searchResultTitle" />

        <!-- Tag / Category -->
        <TextView
            android:id="@+id/searchResultTag"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="HR Access"
            android:textColor="@color/brand_orange"
            android:textSize="12sp"
            android:background="@drawable/bg_rounded_tag"
            android:backgroundTint="@color/bg_orange_light"
            android:paddingHorizontal="12dp"
            android:paddingVertical="6dp"
            android:layout_marginTop="12dp"
            app:drawableStartCompat="@drawable/ic_lock"
            app:drawableTint="@color/brand_orange"
            android:drawablePadding="4dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/searchResultDescription" />

        <!-- Divider -->
        <View
            android:id="@+id/divider"
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="#E0E0E0"
            android:layout_marginTop="12dp"
            app:layout_constraintTop_toBottomOf="@+id/searchResultTag" />

        <!-- Footer: Date and Eye Icon -->
        <TextView
            android:id="@+id/searchResultDate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Last Updated: 2023-11-01"
            android:textColor="@color/text_secondary"
            android:textSize="12sp"
            android:layout_marginTop="12dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/divider" />

        <ImageView
            android:id="@+id/iconEye"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:src="@drawable/ic_eye"
            app:tint="@color/text_primary"
            android:layout_marginTop="12dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/divider"
            app:layout_constraintBottom_toBottomOf="@+id/searchResultDate" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>
